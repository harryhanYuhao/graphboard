//! Virtual workspace root.
//!
//! The real Rust crate lives at `crates/zxw/`. This file exists only
//! so cargo accepts the root `Cargo.toml`'s `[package]` table
//! (`no targets specified` otherwise). 
